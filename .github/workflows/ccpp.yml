name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: make
      run: make fnmatch
    - name: test
      run: test `ls -1 | fnmatch '*.c' | wc -l ` = 1
